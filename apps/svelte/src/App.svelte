<script lang="ts">
  // Scaffold placeholder. The onboarding / backup / locked / unlocked state
  // machine (driven entirely by @wdk-web/wallet-core's public surface) lands
  // in the next commit. Kept valid Svelte 5 so the quartet is honestly green
  // from the first scaffold commit.
  const title = "WDK Wallet — Svelte portability proof";
</script>

<main>
  <h1>{title}</h1>
  <p>Headless <code>@wdk-web/wallet-core</code> consumed by a second framework.</p>
</main>
