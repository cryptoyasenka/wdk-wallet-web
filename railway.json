{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack pnpm install --frozen-lockfile && corepack pnpm --filter @wdk-web/wallet-core build && corepack pnpm --filter next exec next build"
  },
  "deploy": {
    "startCommand": "corepack pnpm --filter next exec next start -p $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
