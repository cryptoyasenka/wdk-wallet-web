# Copy to apps/next/.env.local — NEVER commit the filled file.
# WDK Indexer (live balances / activity). Obtain from Tether WDK docs.
WDK_INDEXER_BASE_URL=
WDK_INDEXER_API_KEY=
# Optional TRON API (only if enabling TRON)
TRON_API_KEY=
TRON_API_SECRET=
